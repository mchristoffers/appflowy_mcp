#!/usr/bin/env bash
# Only for `docker compose config` in the GitHub Action. The real values live
# as Environment Variables at the Coolify app, not here.
APPFLOWY_MCP_TAG=main
APPFLOWY_BASE_URL=https://appflowy.mchristoffers.dev
APPFLOWY_EMAIL=mcp@example.invalid
APPFLOWY_PASSWORD=example
ISSUER=https://appflowymcp-oauth.mchristoffers.dev
GATEWAY_USERNAME=example
GATEWAY_PASSWORD=example